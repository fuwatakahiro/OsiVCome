<% if current_customer != customer %>
<% if params[:follow]%>
  <% if current_customer.following?(customer) %>
    <%= link_to "フォローを外す", customer_relationships_path(customer.id,{follow: "follow"}), remote: true,  method: :delete, class: "btn btn-secondary" %>
  <% else %>
    <%= link_to "フォローをする", customer_relationships_path(customer.id, {follow: "follow"}), remote: true,method: :post, class: "btn btn-primary" %>
  <% end %>
  <% else %>
  <% if current_customer.following?(customer) %>
    <%= link_to "フォローを外す", customer_relationships_path(customer.id), remote: true,  method: :delete, class: "btn btn-secondary" %>
  <% else %>
    <%= link_to "フォローをする", customer_relationships_path(customer.id), remote: true,method: :post, class: "btn btn-primary" %>
  <% end %>
  <% end %>
<% end %>
