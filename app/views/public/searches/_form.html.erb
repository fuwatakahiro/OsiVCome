<%= form_with url: search_path, method: :get do |f| %>
  <%= f.text_field :content, placeholder: "Search" %>
  <%= f.select :model, options_for_select({"ユーザー"=> "customer", "キャラクター" => "character"}), include_blank: "検索選択" %>
  <%= button_tag type: "submit", class: "btn rounded-0 border-0 input-group-btn btn-success" do %>
    <i class="fa fa-search"></i>
  <% end %>
<% end %>