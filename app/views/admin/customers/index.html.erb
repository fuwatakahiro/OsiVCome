<div class="container">
  <div class="row py-3">
    <h3 class="col-lg-5 col-6">ユーザー一覧</h3>
    <div class="ml-lg-auto col-6 px-0"><%= render "admin/searches/form", customers: @customers%></div>
  </div>
  <div class="my-4">
    <%= render "admin/customers/index", customers: @customers %>
  </div>
  <div class="row justify-content-center">
    <%= paginate @customers %>
  </div>
</div>